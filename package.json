{
  "name": "rxjs-overlap-map",
  "version": "0.0.2-1",
  "description": "Alternative operator to RxJS switchMap",
  "main": "dist/rxjs-overlap-map.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "rollup -c",
    "test": "npm run build && mocha --opts mocha.opts",
    "coverage": "npm run build && nyc mocha --opts mocha.opts",
    "deploy": "npm run coverage && npm publish"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "rxjs",
    "operator",
    "overlap"
  ],
  "author": "clitetailor",
  "license": "MIT",
  "peerDependencies": {
    "rxjs": "^6.3.3"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "nyc": "^13.1.0",
    "rollup": "^0.66.6",
    "rollup-plugin-babel": "^3.0.7",
    "rxjs": "^6.3.3"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/clitetailor/rxjs-overlap-map.git"
  },
  "bugs": {
    "url": "https://github.com/clitetailor/rxjs-overlap-map/issues"
  },
  "homepage": "https://github.com/clitetailor/rxjs-overlap-map#readme"
}
